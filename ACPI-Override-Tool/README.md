# These scripts disable the HPET timer and optionally stub GPE interrupt handlers. Automatic extraction of DSDT from the registry, patching, and installation via Windows' native acpitabl.dat override.<br>
# Run INSTALL.bat as Administrator.
